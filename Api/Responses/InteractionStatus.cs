namespace SimpleTransformer.Api.Responses
{
    //Represents the status of an interaction with the transformer model.
    public enum InteractionStatus
    {
        Success = 0,
        Failure = 1,
        Error = 2
    }
}